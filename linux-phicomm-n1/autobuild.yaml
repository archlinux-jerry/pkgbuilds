type:
    auto
timeout:
    720
priority:
    5
extra:
    - update:
        - rm -rf 'src'
        - bash buildbot.update.d/update.sh
    - failure:
        - rm -rf 'src_fail'
        - mv 'src' 'src_fail'
